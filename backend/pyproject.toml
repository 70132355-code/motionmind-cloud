[tool.poetry]
name = "motionmind-backend"
version = "1.0.0"
description = "MotionMind AI Gesture Control Backend"
authors = ["FURQAN FYP"]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.1.2"
gunicorn = "^23.0.0"
opencv-python-headless = "^4.12.0"
mediapipe = "^0.10.31"
numpy = "^2.2.6"
flask-cors = "^6.0.2"
python-pptx = "^1.0.2"
pillow = "^12.1.0"
werkzeug = "^3.1.4"
firebase-admin = "^7.1.0"

[tool.poetry.scripts]
app = "app:app"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
